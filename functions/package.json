{
  "name": "wdig",
  "version": "1.0.0",
  "description": "Where Does It Go?",
  "author": "realprogrammer5000",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "lint": "eslint --ext .js,.vue --ignore-path .gitignore .",
    "deploy": "nuxt build && firebase deploy && curl -X POST \"https://api.cloudflare.com/client/v4/zones/1fa028876ca992f564f34302639490e1/purge_cache\" -H \"Authorization: Bearer $(cat token)\" -H \"Content-Type: application/json\"  --data '{\"purge_everything\":true}'"
  },
  "engines": {
    "node": "10"
  },
  "dependencies": {
    "express": "latest",
    "firebase-admin": "^8.10.0",
    "firebase-functions": "^3.6.2",
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.0.0",
    "@nuxtjs/eslint-config": "^2.0.0",
    "@nuxtjs/eslint-module": "^1.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^6.1.0",
    "eslint-plugin-nuxt": ">=0.4.2",
    "firebase-functions-test": "^0.2.0"
  }
}
